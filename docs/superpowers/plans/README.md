# Implementation plans

Task-by-task build plans, one per feature. Each plan follows the design spec
of the same date and topic.

_None yet. The fork's own upstream-sync procedure lives in
`scripts/sync-upstream.sh`, which carries its rules inline._
